import React from 'react'

export interface InternalSketchRootProps {
    children?: React.ReactElement | React.ReactElement[]
}

export const InternalSketchRoot: React.FC<InternalSketchRootProps> = () => {
  return null
}
